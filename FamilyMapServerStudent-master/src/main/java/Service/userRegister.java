package Service;
import Service.Request.*;
import Service.Response.*;

/**
 * registers a new user
 * returns a message whether successful or not
 */
public class userRegister {
    /**
     * will take the userRegisterRequest - grab the info
     * then check it, if good add it to db then send response
     * @param urr userRegisterRequest
     * @return userRegisterResponse
     */
    public userRegisterResponse register(userRegisterRequest urr)
    {
        //do stuff
        return null;
    }
}
